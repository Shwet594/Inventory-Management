<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Inventory Management</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body { background: linear-gradient(135deg,#f1f5f9 0%,#e2e8f0 100%); }
    .glass { background: rgba(255,255,255,0.7); backdrop-filter: blur(15px); }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center p-6 text-gray-900">

  <div class="glass p-8 rounded-2xl shadow-xl w-full max-w-md">

    <h2 class="text-3xl font-bold mb-4 text-blue-700">Login</h2>

    <% if (loginErrors && loginErrors.length > 0) { %>
      <div class="bg-red-100 text-red-700 p-3 rounded-lg mb-4">
        <% loginErrors.forEach(err => { %>
          <p>• <%= err.msg %></p>
        <% }) %>
      </div>
    <% } %>

    <form action="/auth/login" method="post" class="space-y-4">

      <input type="email" name="email" placeholder="Email"
        value="<%= oldLogin?.email || '' %>"
        class="w-full px-4 py-3 border rounded-lg focus:ring-2">

      <input type="password" name="password" placeholder="Password"
        class="w-full px-4 py-3 border rounded-lg focus:ring-2">

      <button class="w-full bg-blue-500 hover:bg-blue-600 py-3 rounded-lg text-white font-medium">
        Login
      </button>

      <p class="text-sm text-center">
        Don’t have an account? 
        <a href="/" class="text-blue-600 hover:underline">Register</a>
      </p>

    </form>

  </div>

</body>
</html>
